**This is a Cypress test automation**
1. Install node.js
2. Install/update Cypress

**To run:**
1. Open the terminal
2. Open Cypress with the tests customer name:
npx cypress open --env CUSTOMER_NAME='Harry Potter'
